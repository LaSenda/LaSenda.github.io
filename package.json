{
  "name": "lasenda.github.io",
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site",
    "start": "npm run deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "gh-pages": "^4.0.0"
  }
}
